rootProject.name = 'connection'

if (new File(rootDir, '../connection-frontend-requestform').exists()) {
    includeBuild '../connection-frontend-requestform'
}

if (new File(rootDir, '../connection-frontend-admin').exists()) {
    includeBuild '../connection-frontend-admin'
}