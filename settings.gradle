rootProject.name = 'connection-backend'

//TODO: does not work
if (rootProject.hasProperty('includeFrontend')){
	includeBuild '../connection-frontend-requestform'
	includeBuild '../connection-frontend-admin'
}